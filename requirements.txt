# Core Dependencies
python-telegram-bot>=20.0
aiohttp>=3.8.0
asyncio-contextmanager>=1.0.0

# WebSocket Support (NEW - CRITICAL)
websockets>=11.0.3
aiohttp-sse-client>=0.2.1

# Web Framework & API
fastapi>=0.95.0
uvicorn>=0.21.0
starlette>=0.26.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0  # PostgreSQL adapter
alembic>=1.11.0  # Database migrations (NEW)

# Scheduling (NEW - for live updates)
APScheduler>=3.10.0

# Payment Processing
stripe>=5.0.0

# Data Processing & ML
numpy>=1.23.0
pandas>=1.5.0
scikit-learn>=1.2.0
tensorflow>=2.12.0  # For advanced ML models (NEW)

# Environment & Config
python-dotenv>=0.21.0

# Utilities
requests>=2.28.0
beautifulsoup4>=4.11.0
python-dateutil>=2.8.2

# API Integrations (NEW)
betfairlightweight>=2.18.0  # Betfair API
requests-oauthlib>=1.3.1  # OAuth for APIs

# Caching & Performance (NEW)
redis>=4.5.0
hiredis>=2.2.0  # Redis performance boost
aiocache>=0.12.0  # Async caching

# Security (NEW - CRITICAL)
cryptography>=41.0.0  # Encryption
PyJWT>=2.8.0  # JWT tokens for API authentication
slowapi>=0.1.8  # Rate limiting middleware
passlib[bcrypt]>=1.7.4  # Password hashing for secure auth

# Monitoring & Analytics (NEW)
prometheus-client>=0.17.0  # Metrics & monitoring
sentry-sdk>=1.39.0  # Error tracking & reporting
python-json-logger>=2.0.7  # Structured logging

# Code Quality & Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
black>=23.11.0  # Code formatter
flake8>=6.1.0  # Linter
mypy>=1.7.0  # Type checking

# Timezone & Date Handling
pytz>=2023.3

pyjwt>=2.8.0  # JWT tokens
argon2-cffi>=21.3.0  # Password hashing

# Rate Limiting (NEW)
slowapi>=0.1.8

# Testing
pytest>=7.0.0
pytest-asyncio>=0.20.0
pytest-cov>=4.0.0
httpx>=0.24.0  # For testing async APIs

# Development
black>=23.0.0
ruff>=0.0.250
mypy>=1.0.0
pre-commit>=3.0.0

# Logging & Monitoring
sentry-sdk>=1.20.0
python-json-logger>=2.0.7
prometheus-client>=0.17.0  # Metrics (NEW)
